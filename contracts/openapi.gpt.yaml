openapi: 3.1.0
info:
  title: JueZ API Catalogue GPT Action
  version: 0.1.0
  description: Minimal GPT Actions contract for the JueZ API Catalogue. Health is public; Hello and Reddit thread actions
    require Microsoft Entra OAuth.
  license:
    name: Proprietary
    identifier: LicenseRef-Proprietary
servers:
  - url: https://func-api-catalogue-prod-bfjstshehpbfk.azurewebsites.net
    description: Production API
x-repairable-error:
  recVersion: "1.0"
  appliesTo: all-service-generated-http-errors
  contentType: application/problem+json
  analyzerOrder:
    - deterministic
    - llm_assisted_for_diagnostic_uncertain
    - validated_fallback
  llm:
    api: OpenAI Responses API
    enabledByEnv: REPAIRABLE_ERRORS_LLM_ENABLED
    input: sanitized_diagnostic_capsule
    output: RepairableProblem
    requiresSchemaValidation: true
    requiresPolicyGate: true
    modelGeneratedRepairPatchAllowed: false
paths:
  /health:
    get:
      operationId: getHealth
      summary: Check API health
      description: Public health endpoint. No OAuth token is required.
      security: []
      responses:
        "200":
          description: API is reachable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
        "404":
          description: Endpoint was not found on the current host.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: The health response could not be generated.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
  /api/hello:
    get:
      operationId: getHello
      summary: Test authenticated access
      description: Protected endpoint used to confirm the GPT Action OAuth token can call the API.
      security:
        - entraOAuth2:
            - api://97df847a-3e44-4aa7-82ea-557f3dfe0203/catalogue.read
      responses:
        "200":
          description: Authenticated hello response.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HelloResponse"
        "401":
          description: Missing or invalid OAuth bearer token.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "403":
          description: Valid token, but caller, tenant, client, scope, or role is not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "500":
          description: The authenticated response could not be generated.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
  /api/reddit/thread:
    post:
      operationId: postRedditThread
      summary: Fetch a Reddit thread
      description: Protected endpoint that fetches a Reddit post and comments server-side. The GPT authenticates to this API,
        not to Reddit.
      security:
        - entraOAuth2:
            - api://97df847a-3e44-4aa7-82ea-557f3dfe0203/reddit.read
      x-repairable-error:
        recVersion: "1.0"
        analyzer:
          supportedModes:
            - deterministic
            - llm_assisted
            - fallback
          defaultMode: deterministic
          llm:
            enabledByEnv: REPAIRABLE_ERRORS_LLM_ENABLED
            input: sanitized_diagnostic_capsule
            output: RepairableProblem
            requiresSchemaValidation: true
            requiresPolicyGate: true
        aliases:
          url: post
          redditUrl: post
          reddit_url: post
          threadUrl: post
          thread_url: post
        retry:
          idempotencyRequired: false
        acceptedInputFormats:
          post:
            - raw Reddit article ID
            - raw Reddit comment ID, resolved to its parent post thread
            - t3 fullname
            - redd.it URL
            - reddit.com comments URL
            - old.reddit.com comments URL
            - Reddit /r/<subreddit>/s/<token> share URL
        security:
          diagnosticCapsulePolicy: sanitized-only
          publicDebugDetailsDefault: false
          exposeRawRequestBody: false
          exposeHeaders: false
          exposeTokens: false
          exposeStackTrace: false
          exposeRawUpstreamBody: false
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RedditThreadRequest"
      responses:
        "200":
          description: Normalized Reddit thread snapshot.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedditThreadResponse"
        "400":
          description: Invalid request payload.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
              examples:
                invalidJson:
                  summary: Invalid JSON body
                  value:
                    type: https://api.juez.local/problems/reddit-thread/caller_contract_violation
                    title: Request contract violation
                    status: 400
                    detail: Request body must be valid JSON.
                    instance: urn:diagnostic:diag_example_invalid_json
                    rec_version: "1.0"
                    operation_id: postRedditThread
                    diagnostic_id: diag_example_invalid_json
                    classification: caller_contract_violation
                    repairable: true
                    confidence: 0.82
                    retry_policy:
                      can_retry: true
                      same_request: false
                    caller_instruction: Send valid JSON with a post field containing a Reddit article ID, t3 fullname, redd.it URL, or
                      canonical reddit.com /comments/<id> URL.
                    safe_debug_summary: Sanitized invalid JSON diagnosis.
                    analysis_mode: deterministic
                aliasUrlInsteadOfPost:
                  summary: Alias URL supplied instead of post
                  value:
                    type: https://api.juez.local/problems/reddit-thread/caller_contract_violation
                    title: Request contract violation
                    status: 400
                    detail: The request used an alias field instead of post.
                    instance: urn:diagnostic:diag_example_alias
                    rec_version: "1.0"
                    operation_id: postRedditThread
                    diagnostic_id: diag_example_alias
                    classification: caller_contract_violation
                    repairable: true
                    confidence: 0.9
                    retry_policy:
                      can_retry: true
                      same_request: false
                    repair_patch:
                      - op: add
                        path: /post
                        value: https://www.reddit.com/r/example/comments/abc123/title/
                      - op: remove
                        path: /url
                    caller_instruction: Move the Reddit URL into post and retry.
                    safe_debug_summary: Sanitized alias-field contract diagnosis.
                    analysis_mode: deterministic
                unresolvedShareUrl:
                  summary: Unresolved Reddit share URL
                  value:
                    type: https://api.juez.local/problems/reddit-thread/caller_contract_violation
                    title: Request contract violation
                    status: 400
                    detail: Reddit /s/ share URLs must resolve to a canonical comments URL before this endpoint can fetch the thread.
                    instance: urn:diagnostic:diag_example_share
                    rec_version: "1.0"
                    operation_id: postRedditThread
                    diagnostic_id: diag_example_share
                    classification: caller_contract_violation
                    repairable: true
                    confidence: 0.82
                    retry_policy:
                      can_retry: true
                      same_request: false
                    repair_plan:
                      - action: replace_invalid_value
                        path: /post
                        value_hint: canonical /comments/<id> URL, redd.it URL, t3 fullname, raw article ID, or raw comment ID
                        reason: The /s/ share URL could not be resolved deterministically.
                    caller_instruction: Do not retry the same /s/ share URL. Use a canonical reddit.com /comments/<id> URL, redd.it URL, t3
                      fullname, or raw article/comment ID instead.
                    safe_debug_summary: Sanitized unresolved share URL diagnosis.
                    analysis_mode: deterministic
        "401":
          description: Missing or invalid OAuth bearer token.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "403":
          description: Valid token, but caller, tenant, client, scope, or role is not allowed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "404":
          description: Reddit content was not found.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
              examples:
                redditNotFound:
                  summary: Reddit content not found
                  value:
                    type: https://api.juez.local/problems/reddit-thread/resource_not_found
                    title: Reddit content was not found
                    status: 404
                    detail: Reddit content was not found for thread.
                    instance: urn:diagnostic:diag_example_404
                    rec_version: "1.0"
                    operation_id: postRedditThread
                    diagnostic_id: diag_example_404
                    classification: resource_not_found
                    repairable: true
                    confidence: 0.68
                    retry_policy:
                      can_retry: true
                      same_request: false
                    caller_instruction: Check that post references an existing public Reddit thread and retry with the corrected identifier
                      or canonical URL.
                    safe_debug_summary: Sanitized Reddit 404 diagnosis.
                    analysis_mode: deterministic
        "413":
          description: Request body exceeds the 65536-byte limit.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "429":
          description: Reddit rate limit or upstream throttling.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
              examples:
                redditRateLimited:
                  summary: Reddit rate limited
                  value:
                    type: https://api.juez.local/problems/reddit-thread/capacity_or_timeout
                    title: Reddit rate limit reached
                    status: 429
                    detail: Reddit rate-limited the request.
                    instance: urn:diagnostic:diag_example_429
                    rec_version: "1.0"
                    operation_id: postRedditThread
                    diagnostic_id: diag_example_429
                    classification: capacity_or_timeout
                    repairable: false
                    confidence: 0.68
                    retry_policy:
                      can_retry: true
                      same_request: true
                      retry_after_ms: 30000
                    repair_plan:
                      - action: retry_later
                        reason: The upstream service asked callers to slow down.
                    caller_instruction: Retry later with the same request. Do not change request parameters solely to bypass rate limiting.
                    safe_debug_summary: Sanitized Reddit rate-limit diagnosis.
                    analysis_mode: deterministic
        "502":
          description: Reddit upstream request failed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
              examples:
                upstreamDependencyFailure:
                  summary: Reddit upstream dependency failure
                  value:
                    type: https://api.juez.local/problems/reddit-thread/dependency_failure
                    title: Reddit dependency failure
                    status: 502
                    detail: Reddit upstream request failed.
                    instance: urn:diagnostic:diag_example_502
                    rec_version: "1.0"
                    operation_id: postRedditThread
                    diagnostic_id: diag_example_502
                    classification: dependency_failure
                    repairable: false
                    confidence: 0.68
                    retry_policy:
                      can_retry: true
                      same_request: true
                    repair_plan:
                      - action: retry_later
                        reason: The failure happened while contacting or processing the upstream Reddit dependency.
                    caller_instruction: Retry later with the same request. Do not invent alternative request parameters for this upstream
                      failure.
                    safe_debug_summary: Sanitized Reddit dependency failure diagnosis.
                    analysis_mode: deterministic
  /api/wlh/categories/top:
    get:
      operationId: getWlhCategoriesTop
      summary: List top wlh categories
      security:
        - entraOAuth2:
            - api://97df847a-3e44-4aa7-82ea-557f3dfe0203/wlh.read
      responses:
        "200":
          description: Top-level categories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WlhCategory"
              examples:
                syntheticTopCategories:
                  summary: Synthetic top-level WLH categories
                  value:
                    - id: "1000"
                      label: "Home and Garden"
                      path: "Home and Garden"
                      depth: 0
                      hitCount: 120
                      hasChildren: true
                      url: "https://example.invalid/wlh/categories/1000"
                    - id: "2000"
                      label: "Electronics"
                      path: "Electronics"
                      depth: 0
                      hitCount: 85
                      hasChildren: true
                      url: "https://example.invalid/wlh/categories/2000"
        "401":
          description: Unauthorized
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "403":
          description: Forbidden
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
  /api/wlh/categories/{categoryId}:
    get:
      operationId: getWlhCategory
      summary: Get one wlh category
      security:
        - entraOAuth2:
            - api://97df847a-3e44-4aa7-82ea-557f3dfe0203/wlh.read
      parameters:
        - name: categoryId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Category
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WlhCategory"
              examples:
                syntheticCategory:
                  summary: Synthetic WLH category
                  value:
                    id: "1100"
                    label: "Kitchen"
                    path: "Home and Garden > Kitchen"
                    depth: 1
                    parentId: "1000"
                    hitCount: 42
                    hasChildren: true
                    url: "https://example.invalid/wlh/categories/1100"
        "401":
          description: Unauthorized
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "403":
          description: Forbidden
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "404":
          description: Not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
  /api/wlh/categories/{categoryId}/children:
    get:
      operationId: getWlhCategoryChildren
      summary: List child wlh categories
      security:
        - entraOAuth2:
            - api://97df847a-3e44-4aa7-82ea-557f3dfe0203/wlh.read
      parameters:
        - name: categoryId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Child categories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WlhCategory"
              examples:
                syntheticChildCategories:
                  summary: Synthetic child WLH categories
                  value:
                    - id: "1110"
                      label: "Small Appliances"
                      path: "Home and Garden > Kitchen > Small Appliances"
                      depth: 2
                      parentId: "1100"
                      hitCount: 18
                      hasChildren: false
                      url: "https://example.invalid/wlh/categories/1110"
                    - id: "1120"
                      label: "Cookware"
                      path: "Home and Garden > Kitchen > Cookware"
                      depth: 2
                      parentId: "1100"
                      hitCount: 24
                      hasChildren: false
                      url: "https://example.invalid/wlh/categories/1120"
        "401":
          description: Unauthorized
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "403":
          description: Forbidden
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "404":
          description: Not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
  /api/wlh/search:
    post:
      operationId: postWlhSearch
      summary: Search wlh offers
      security:
        - entraOAuth2:
            - api://97df847a-3e44-4aa7-82ea-557f3dfe0203/wlh.read
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WlhSearchRequest"
            examples:
              syntheticSearchRequest:
                summary: Synthetic WLH search request
                value:
                  categoryId: "1110"
                  keyword: "coffee grinder"
                  priceFrom: 10
                  priceTo: 80
                  areaId: "900"
                  paylivery: true
                  rows: 2
                  page: 1
                  condition: used
                  delivery:
                    - pickup
                    - shipping
                  requiredTerms:
                    - grinder
      responses:
        "200":
          description: Search results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WlhSearchResponse"
              examples:
                syntheticSearchResults:
                  summary: Synthetic WLH search response
                  value:
                    source: wlh
                    fetchedAt: "2026-05-30T12:00:00.000Z"
                    sourceUrl: "https://example.invalid/wlh/search?category=1110&keyword=coffee%20grinder"
                    rowsFound: 2
                    rowsReturned: 2
                    filteredRowsReturned: 1
                    category:
                      id: "1110"
                      label: "Small Appliances"
                      path: "Home and Garden > Kitchen > Small Appliances"
                      depth: 2
                      parentId: "1100"
                      hitCount: 18
                      hasChildren: false
                      url: "https://example.invalid/wlh/categories/1110"
                    results:
                      - id: "SYNTHETIC-AD-1001"
                        title: "Manual coffee grinder"
                        priceAmount: 25
                        priceDisplay: "25 EUR"
                        location: "Sample District"
                        postcode: "0000"
                        state: "sample-state"
                        publishedAt: "2026-05-29T09:30:00.000Z"
                        changedAt: "2026-05-29T10:15:00.000Z"
                        paylivery: true
                        sellerId: "SYNTHETIC-SELLER-1"
                        url: "https://example.invalid/wlh/offers/SYNTHETIC-AD-1001"
                        imageCount: 2
                        thumbnailUrl: "https://example.invalid/images/SYNTHETIC-AD-1001-thumb.jpg"
        "400":
          description: Bad request
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "401":
          description: Unauthorized
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "403":
          description: Forbidden
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "404":
          description: Not found
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "413":
          description: Request body exceeds the 65536-byte limit.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "429":
          description: Rate limited
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "502":
          description: Upstream failure
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
  /api/wlh/offers/{adId}:
    get:
      operationId: getWlhOffer
      summary: Get wlh offer detail
      security:
        - entraOAuth2:
            - api://97df847a-3e44-4aa7-82ea-557f3dfe0203/wlh.read
      parameters:
        - name: adId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Offer detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WlhOfferDetail"
              examples:
                syntheticOfferDetail:
                  summary: Synthetic WLH offer detail
                  value:
                    id: "SYNTHETIC-AD-1001"
                    title: "Manual coffee grinder"
                    description: "Synthetic listing description for a used manual coffee grinder."
                    priceAmount: 25
                    priceDisplay: "25 EUR"
                    location: "Sample District"
                    postcode: "0000"
                    state: "sample-state"
                    publishedAt: "2026-05-29T09:30:00.000Z"
                    changedAt: "2026-05-29T10:15:00.000Z"
                    status: active
                    seller:
                      id: "SYNTHETIC-SELLER-1"
                      name: "Synthetic Seller"
                    paylivery: true
                    images:
                      - id: "SYNTHETIC-AD-1001-IMG-1"
                        url: "https://example.invalid/images/SYNTHETIC-AD-1001-1.jpg"
                      - id: "SYNTHETIC-AD-1001-IMG-2"
                        url: "https://example.invalid/images/SYNTHETIC-AD-1001-2.jpg"
                    deliveryOptions:
                      - carrier: "Synthetic Parcel"
                        parcelSize: small
                        price: 4.99
                        originalPrice: 5.99
                        description: "Synthetic delivery option."
                        deliveryDays: "2-4"
                        insurance: true
        "401":
          description: Unauthorized
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "403":
          description: Forbidden
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "429":
          description: Rate limited
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "502":
          description: Upstream failure
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
  /api/wlh/offers/{adId}/images:
    get:
      operationId: getWlhOfferImages
      summary: Get wlh offer images
      security:
        - entraOAuth2:
            - api://97df847a-3e44-4aa7-82ea-557f3dfe0203/wlh.read
      parameters:
        - name: adId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Offer images
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  source:
                    type: string
                    description: Source system marker (`wlh`).
                  adId:
                    type: string
                  images:
                    type: array
                    items:
                      $ref: "#/components/schemas/WlhImage"
                required:
                  - source
                  - adId
                  - images
              examples:
                syntheticOfferImages:
                  summary: Synthetic WLH offer images
                  value:
                    source: wlh
                    adId: "SYNTHETIC-AD-1001"
                    images:
                      - id: "SYNTHETIC-AD-1001-IMG-1"
                        url: "https://example.invalid/images/SYNTHETIC-AD-1001-1.jpg"
                      - id: "SYNTHETIC-AD-1001-IMG-2"
                        url: "https://example.invalid/images/SYNTHETIC-AD-1001-2.jpg"
        "401":
          description: Unauthorized
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "403":
          description: Forbidden
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "429":
          description: Rate limited
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "502":
          description: Upstream failure

          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
  /api/reddit/comment-tree:
    post:
      operationId: postRedditCommentTree
      summary: Fetch a bounded Reddit comment block
      description: Use after `/api/reddit/thread` when a large thread needs staged loading. Send either `commentId` for one
        subtree or `children` from a returned continuation handle.
      security:
        - entraOAuth2:
            - api://97df847a-3e44-4aa7-82ea-557f3dfe0203/reddit.read
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RedditCommentTreeRequest"
      responses:
        "200":
          description: Normalized bounded Reddit comment block.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedditCommentTreeResponse"
        "400":
          description: Invalid request payload.
        "413":
          description: Request body exceeds the 65536-byte limit.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "401":
          description: Unauthorized.
        "403":
          description: Forbidden.
        "404":
          description: Reddit content not found.
        "429":
          description: Reddit rate limited the backend.
        "502":
          description: Reddit dependency failure.
  /api/reddit/thread/overview:
    post:
      operationId: postRedditThreadOverview
      summary: Fetch Reddit thread overview metadata
      description: Cheap thread metadata and coverage summary used to choose a small/medium/large analysis strategy before
        fetching bodies.
      security: &a1
        - entraOAuth2:
            - api://97df847a-3e44-4aa7-82ea-557f3dfe0203/reddit.read
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RedditThreadOverviewRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedditThreadOverviewResponse"
        "400":
          description: Invalid request payload.
        "413":
          description: Request body exceeds the 65536-byte limit.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "401":
          description: Unauthorized.
        "403":
          description: Forbidden.
        "404":
          description: Reddit content not found.
        "429":
          description: Reddit rate limited the backend.
        "502":
          description: Reddit dependency failure.
  /api/reddit/thread/comments:
    post:
      operationId: postRedditThreadComments
      summary: Page Reddit comment skeletons
      description: Returns lightweight, paginated comment skeletons with optional bodies, previews, mechanical filters, cursor
        pagination, maxBytes, and coverage metadata.
      security: *a1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RedditCommentQueryRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedditCommentQueryResponse"
        "400":
          description: Invalid request payload.
        "413":
          description: Request body exceeds the 65536-byte limit.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "401":
          description: Unauthorized.
        "403":
          description: Forbidden.
        "404":
          description: Reddit content not found.
        "429":
          description: Reddit rate limited the backend.
        "502":
          description: Reddit dependency failure.
  /api/reddit/comments/batch:
    post:
      operationId: postRedditCommentsBatch
      summary: Fetch selected Reddit comments by ID
      description: Fetches full selected comments by ID with field projection so GPT can hydrate only high-signal comments
        after inspecting skeleton pages.
      security: *a1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RedditCommentsBatchRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedditCommentsBatchResponse"
        "400":
          description: Invalid request payload.
        "413":
          description: Request body exceeds the 65536-byte limit.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/RepairableProblem"
        "401":
          description: Unauthorized.
        "403":
          description: Forbidden.
        "404":
          description: Reddit content not found.
        "429":
          description: Reddit rate limited the backend.
        "502":
          description: Reddit dependency failure.
  /api/bring/lists:
    get:
      operationId: bringListLists
      summary: List Bring! shopping lists
      description: Protected, read-only view of allowlisted Bring! lists. Credentials, tokens, account data, and cache locations are never exposed.
      tags: [Bring]
      security:
        - entraOAuth2: [api://97df847a-3e44-4aa7-82ea-557f3dfe0203/bring.read]
      responses:
        "200":
          description: Normalized shopping lists.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BringListsResponse" }
        "401":
          description: Missing or invalid API OAuth bearer token.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "403":
          description: Authenticated caller is not authorized, or the confirmation is invalid, expired, or bound to another identity.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "502":
          description: Bring authentication, dependency, or undocumented response-shape failure.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
  /api/bring/lists/{listUuid}/items:
    parameters:
      - $ref: "#/components/parameters/BringListUuid"
    get:
      operationId: bringGetItems
      summary: Get Bring! shopping-list items
      description: Protected read operation returning normalized items and a SHA-256 list version for optimistic concurrency.
      tags: [Bring]
      security:
        - entraOAuth2: [api://97df847a-3e44-4aa7-82ea-557f3dfe0203/bring.read]
      responses:
        "200":
          description: Normalized list items.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BringList" }
        "401":
          description: Missing or invalid API OAuth bearer token.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "403":
          description: Authenticated caller is not authorized for Bring operations.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "404":
          description: Bring list not found.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "502":
          description: Bring authentication, dependency, or undocumented response-shape failure.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
    post:
      operationId: bringAddItems
      summary: Idempotently add a batch of Bring! items
      description: Adds 1-50 items to an explicitly allowlisted own list. Generate a new UUID for `operationId`; retry only the identical action, list, payload, and tenant-bound principal with the same UUID.
      tags: [Bring]
      security:
        - entraOAuth2: [api://97df847a-3e44-4aa7-82ea-557f3dfe0203/bring.write]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BringAddRequest" }
      responses:
        "200":
          description: Batch accepted by Bring!.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BringMutationResponse" }
        "400":
          description: Invalid batch or UUID.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "401":
          description: Missing or invalid API OAuth bearer token.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "403":
          description: Authenticated caller is not authorized for Bring operations.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "409":
          description: Idempotency or legacy-record conflict, stale list version, or ambiguous prior outcome.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "410":
          description: The stored successful-result replay window has expired.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "502":
          description: Bring authentication, dependency, or undocumented response-shape failure.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
  /api/bring/lists/{listUuid}/mutations/prepare:
    parameters:
      - $ref: "#/components/parameters/BringListUuid"
    post:
      operationId: bringPrepareItemMutation
      summary: Prepare a destructive Bring! item mutation
      description: Validates a complete or remove request without changing Bring! and returns a five-minute token bound to the tenant-aware principal, action, list, payload, nonce, and expiry. Never reuse the operation ID for another action.
      tags: [Bring]
      security:
        - entraOAuth2: [api://97df847a-3e44-4aa7-82ea-557f3dfe0203/bring.complete]
        - entraOAuth2: [api://97df847a-3e44-4aa7-82ea-557f3dfe0203/bring.remove]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BringPrepareMutationRequest" }
      responses:
        "200":
          description: Mutation prepared; no upstream write has occurred.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BringPreparedMutationResponse" }
        "400":
          description: Invalid batch or UUID.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "401":
          description: Missing or invalid API OAuth bearer token.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "403":
          description: Authenticated caller is not authorized for Bring operations.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "409":
          description: Action/input/identity idempotency conflict, legacy-record conflict, or stale list version.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "410":
          description: The prepared confirmation replay window has expired.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
  /api/bring/lists/{listUuid}/mutations/apply:
    parameters:
      - $ref: "#/components/parameters/BringListUuid"
    post:
      operationId: bringApplyItemMutation
      summary: Apply a confirmed destructive Bring! item mutation
      description: Applies a prepared complete or remove operation using its tenant-aware identity-, action-, list-, and payload-bound confirmation token. After success, only the exact consumed token can replay the recorded result without another upstream mutation.
      tags: [Bring]
      security:
        - entraOAuth2: [api://97df847a-3e44-4aa7-82ea-557f3dfe0203/bring.complete]
        - entraOAuth2: [api://97df847a-3e44-4aa7-82ea-557f3dfe0203/bring.remove]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BringApplyMutationRequest" }
      responses:
        "200":
          description: Mutation applied, or the exact consumed-token result replayed without another upstream call.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BringMutationResponse" }
        "400":
          description: Invalid UUID, malformed confirmation token, or operation ID from an unsupported legacy integrity record.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "401":
          description: Missing or invalid API OAuth bearer token.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "403":
          description: Authenticated caller is not authorized for Bring operations.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "409":
          description: Idempotency conflict or ambiguous prior outcome.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "410":
          description: The stored successful-result replay window has expired.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
        "502":
          description: Bring authentication, dependency, or undocumented response-shape failure.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/RepairableProblem" }
components:
  securitySchemes:
    entraOAuth2:
      type: oauth2
      description: Microsoft Entra OAuth 2.0 authorization-code flow for the dedicated ChatGPT Action client.
      flows:
        authorizationCode:
          authorizationUrl: https://login.microsoftonline.com/7ac3dfd6-e810-4693-805a-9535eb3ab166/oauth2/v2.0/authorize
          tokenUrl: https://login.microsoftonline.com/7ac3dfd6-e810-4693-805a-9535eb3ab166/oauth2/v2.0/token
          scopes:
            api://97df847a-3e44-4aa7-82ea-557f3dfe0203/catalogue.read: Read the authenticated local API catalogue.
            api://97df847a-3e44-4aa7-82ea-557f3dfe0203/reddit.read: Read normalized Reddit content.
            api://97df847a-3e44-4aa7-82ea-557f3dfe0203/wlh.read: Read normalized willhaben content.
            api://97df847a-3e44-4aa7-82ea-557f3dfe0203/bring.read: Read explicitly allowlisted Bring! lists.
            api://97df847a-3e44-4aa7-82ea-557f3dfe0203/bring.write: Add items to explicitly writable Bring! lists.
            api://97df847a-3e44-4aa7-82ea-557f3dfe0203/bring.complete: Prepare and apply item completion.
            api://97df847a-3e44-4aa7-82ea-557f3dfe0203/bring.remove: Prepare and apply item removal.
  parameters:
    BringListUuid:
      name: listUuid
      in: path
      required: true
      description: Bring list UUID. HTTP routes require an explicit list; MCP/service callers may omit it to use the configured/account default.
      schema: { type: string, format: uuid }
  schemas:
    BringItemInput:
      type: object
      additionalProperties: false
      required: [name]
      properties:
        name: { type: string, minLength: 1, maxLength: 200 }
        specification: { type: string, maxLength: 500 }
        uuid: { type: string, format: uuid }
    BringMutationItems:
      type: array
      minItems: 1
      maxItems: 50
      items: { $ref: "#/components/schemas/BringItemInput" }
    BringAddRequest:
      type: object
      additionalProperties: false
      required: [operationId, items]
      properties:
        operationId: { type: string, format: uuid }
        expectedListVersion: { type: string, pattern: "^[0-9a-f]{64}$" }
        items: { $ref: "#/components/schemas/BringMutationItems" }
    BringPrepareMutationRequest:
      type: object
      additionalProperties: false
      required: [operationId, operation, items]
      properties:
        operationId:
          type: string
          format: uuid
          description: Action-, payload-, list-, and principal-bound idempotency key; never reuse it for another action.
        expectedListVersion: { type: string, pattern: "^[0-9a-f]{64}$" }
        operation: { type: string, enum: [complete, remove] }
        items: { $ref: "#/components/schemas/BringMutationItems" }
    BringApplyMutationRequest:
      type: object
      additionalProperties: false
      required: [operationId, confirmationToken]
      properties:
        operationId: { type: string, format: uuid }
        confirmationToken:
          type: string
          minLength: 1
          maxLength: 4096
          description: Opaque token returned by prepare. The exact consumed token is required for a successful-result replay.
    BringListSummary:
      type: object
      additionalProperties: false
      required: [uuid, name, isDefault, shared]
      properties:
        uuid: { type: string, format: uuid }
        name: { type: string }
        theme: { type: string }
        isDefault: { type: boolean }
        shared: { type: boolean, description: True when Bring marks or exposes the list as shared. }
    BringListsResponse:
      type: object
      additionalProperties: false
      required: [source, lists]
      properties:
        source: { type: string, const: bring }
        lists:
          type: array
          items: { $ref: "#/components/schemas/BringListSummary" }
    BringItem:
      allOf:
        - $ref: "#/components/schemas/BringItemInput"
        - type: object
          required: [status]
          properties:
            status: { type: string, enum: [active, completed] }
    BringList:
      type: object
      additionalProperties: false
      required: [uuid, version, items]
      properties:
        uuid: { type: string, format: uuid }
        name: { type: string }
        version: { type: string, pattern: "^[0-9a-f]{64}$" }
        items:
          type: array
          items: { $ref: "#/components/schemas/BringItem" }
    BringPreparedMutationResponse:
      type: object
      additionalProperties: false
      required: [source, state, operationId, operation, listPseudonym, itemCount, expiresAt, confirmationToken, replayed]
      properties:
        source: { type: string, const: bring }
        state: { type: string, const: prepared }
        operationId: { type: string, format: uuid }
        operation: { type: string, enum: [complete, remove] }
        listPseudonym: { type: string }
        itemCount: { type: integer, minimum: 1, maximum: 50 }
        expiresAt: { type: string, format: date-time }
        confirmationToken:
          type: string
          description: Opaque five-minute token bound to tenant-aware principal, action, list, payload, nonce, and expiry.
        replayed: { type: boolean }
    BringMutationResponse:
      type: object
      additionalProperties: false
      required: [source, listUuid, operation, operationId, itemCount, state, replayed]
      properties:
        source: { type: string, const: bring }
        listUuid: { type: string, format: uuid }
        operation: { type: string, enum: [add, complete, remove] }
        operationId: { type: string, format: uuid }
        itemCount: { type: integer, minimum: 1, maximum: 50 }
        state: { type: string, const: succeeded }
        replayed: { type: boolean }
    WlhCategory:
      type: object
      additionalProperties: false
      properties:
        id: { type: string, description: Category identifier. }
        label: { type: string, description: Category display label. }
        path: { type: string, description: Category hierarchy path. }
        depth: { type: number, description: Category depth in tree. }
        parentId: { type: string, description: Parent category identifier. }
        hitCount: { type: number, description: Approximate listing count. }
        hasChildren: { type: boolean, description: True when child categories exist. }
        url: { type: string, description: Category route path resolved against WLH_BASE_URL. }
      required: [id, label, path, depth, hasChildren]
    WlhSearchRequest:
      type: object
      additionalProperties: false
      properties:
        categoryId: { type: string, description: Required category identifier for search. }
        keyword: { type: string, description: Optional free-text search query. }
        priceFrom: { type: number, description: Optional lower price bound. }
        priceTo: { type: number, description: Optional upper price bound. }
        areaId: { type: string, description: Optional area identifier. }
        paylivery: { type: boolean, description: Restrict to listings with paylivery support. }
        rows: { type: integer, minimum: 1, maximum: 100, description: Page size (default 30). }
        page: { type: integer, minimum: 1, description: Page number (default 1). }
        condition: { type: string, enum: [new, like_new, used, defect], description: Typed condition filter. }
        delivery:
          type: array
          items: { type: string, enum: [pickup, shipping] }
          description: Typed delivery filters.
        requiredTerms:
          type: array
          items: { type: string }
          description: Post-filter terms; all non-empty terms must match title/body.
      required: [categoryId]
    WlhImage:
      type: object
      additionalProperties: false
      properties:
        id: { type: string, description: Listing identifier. }
        url: { type: string, description: Canonical listing URL resolved from runtime base URL. }
      required: [id, url]
    WlhSearchResult:
      type: object
      additionalProperties: false
      properties:
        id: { type: string, description: Listing identifier. }
        title: { type: string, description: Listing title. }
        priceAmount: { type: [number, "null"], description: Parsed numeric price amount when available. }
        priceDisplay: { type: string, description: Human-readable price string. }
        location: { type: string, description: Location label for the listing/offer. }
        postcode: { type: string, description: Postal code when available. }
        state: { type: string, description: Region/state value when available. }
        publishedAt: { type: string, description: Published timestamp from upstream payload. }
        changedAt: { type: string, description: Last changed timestamp from upstream payload. }
        paylivery: { type: [boolean, "null"], description: True when paylivery options are detected. }
        sellerId: { type: string, description: Seller identifier when present. }
        url: { type: string, description: Canonical listing URL resolved from runtime base URL. }
        imageCount: { type: number, description: Number of known images for the listing. }
        thumbnailUrl: { type: string, description: Preferred thumbnail URL when available. }
      required: [id, title, priceAmount, priceDisplay, location, url, imageCount]
    WlhSearchResponse:
      type: object
      additionalProperties: false
      properties:
        source: { type: string, description: Source system marker (`wlh`). }
        fetchedAt: { type: string, description: UTC fetch timestamp. }
        sourceUrl: { type: string, description: Source URL used for upstream fetch. }
        rowsFound: { type: [number, "null"], description: Upstream reported total rows found. }
        rowsReturned: { type: number, description: Raw rows returned before requiredTerms filtering. }
        filteredRowsReturned: { type: number, description: Rows returned after requiredTerms filtering. }
        category: { $ref: "#/components/schemas/WlhCategory" }
        results:
          type: array
          items: { $ref: "#/components/schemas/WlhSearchResult" }
      required: [source, fetchedAt, sourceUrl, rowsFound, rowsReturned, filteredRowsReturned, category, results]
    WlhOfferDetail:
      type: object
      additionalProperties: false
      properties:
        id: { type: string, description: Listing identifier. }
        title: { type: string, description: Listing title. }
        description: { type: string, description: HTML-cleaned offer description text. }
        priceAmount: { type: [number, "null"], description: Parsed numeric price amount when available. }
        priceDisplay: { type: string, description: Human-readable price string. }
        location: { type: string, description: Location label for the listing/offer. }
        postcode: { type: string, description: Postal code when available. }
        state: { type: string, description: Region/state value when available. }
        publishedAt: { type: string, description: Published timestamp from upstream payload. }
        changedAt: { type: string, description: Last changed timestamp from upstream payload. }
        status: { type: string, description: Offer status identifier. }
        seller:
          type: object
          additionalProperties: false
          properties:
            id: { type: string, description: Listing identifier. }
            name: { type: string }
          required: [id, name]
        paylivery: { type: [boolean, "null"], description: True when paylivery options are detected. }
        images:
          type: array
          items: { $ref: "#/components/schemas/WlhImage" }
        deliveryOptions:
          type: array
          items:
            type: object
            additionalProperties: false
            properties:
              carrier: { type: string, description: Delivery carrier display name. }
              parcelSize: { type: string, description: Parcel size label. }
              price: { description: Delivery price payload value. }
              originalPrice: { description: Original delivery price payload value. }
              description: { type: string, description: HTML-cleaned offer description text. }
              deliveryDays: { description: Delivery day range payload value. }
              insurance: { description: Delivery insurance payload value. }
      required: [id, title, description, priceAmount, priceDisplay, location, images]

    ErrorResponse:
      type: object
      additionalProperties: true
      required:
        - error
      properties:
        error:
          type: string
          description: Machine-readable error code for structured input errors, or a human-readable error summary for other non-auth application errors.
          examples:
            - UNRESOLVED_REDDIT_SHARE_URL
    HealthResponse:
      type: object
      additionalProperties: false
      properties:
        status:
          type: string
        service:
          type: string
        timestamp:
          type: string
          format: date-time
        environmentName:
          type: string
          enum:
            - test
            - prod
            - local
            - unknown
        deployedCommitSha:
          type: string
        deployedSourceRef:
          type: string
        deploymentRunId:
          type: string
        deployedAtUtc:
          type: string
        buildTimestampUtc:
          type: string
      required:
        - status
        - service
        - timestamp
        - environmentName
        - deployedCommitSha
        - deployedSourceRef
        - deploymentRunId
        - deployedAtUtc
        - buildTimestampUtc
    HelloResponse:
      type: object
      additionalProperties: true
      properties:
        message:
          type: string
        authenticated:
          type: boolean
        user:
          type: object
          additionalProperties: true
      required:
        - message
        - authenticated
    RedditThreadRequest:
      type: object
      additionalProperties: false
      properties:
        post:
          type: string
          description: Reddit post URL, Reddit comments URL, Reddit /s/ share URL, Reddit article ID, or Reddit comment ID.
        sort:
          type: string
          enum:
            - confidence
            - top
            - new
            - controversial
            - old
            - qa
          default: confidence
        maxComments:
          type: integer
          minimum: 1
          maximum: 10000
          default: 10000
          description: Maximum number of normalized comments to return. Large full-body responses can exceed GPT/tool response limits; for medium or large threads prefer the overview, paginated comments, batch hydration, and comment-tree endpoints.
        maxMoreChildrenRequests:
          type: integer
          minimum: 0
          maximum: 10
          default: 0
      required:
        - post
    RedditThreadResponse:
      type: object
      additionalProperties: true
      properties:
        source:
          type: string
        fetchedAt:
          type: string
          format: date-time
        input:
          type: string
        post:
          type: object
          additionalProperties: true
        comments:
          type: array
          items:
            type: object
            additionalProperties: true
        commentContinuations:
          type: array
          items:
            type: object
            additionalProperties: true
        warnings:
          type: array
          items:
            type: string
      required:
        - source
        - fetchedAt
        - input
        - post
        - comments
    RedditCommentTreeRequest:
      type: object
      additionalProperties: false
      properties:
        post:
          type: string
        commentId:
          type: string
          description: Raw comment ID or t1 fullname. Exactly one of commentId or children is required.
        children:
          oneOf:
            - type: string
            - type: array
              items:
                type: string
          description: Child IDs from a continuation handle. Exactly one of commentId or children is required.
        parentId:
          type: string
        sort:
          type: string
          enum:
            - confidence
            - top
            - new
            - controversial
            - old
            - qa
        depth:
          type: integer
          minimum: 0
          maximum: 10
          default: 3
        limit:
          type: integer
          minimum: 1
          maximum: 500
          default: 100
        maxMoreChildrenRequests:
          type: integer
          minimum: 0
          maximum: 10
          default: 0
      required:
        - post
    RedditCommentTreeResponse:
      type: object
      additionalProperties: true
      properties:
        source:
          type: string
        fetchedAt:
          type: string
          format: date-time
        input:
          type: string
        post:
          type: object
          additionalProperties: true
        mode:
          type: string
          enum:
            - comment
            - children
        comments:
          type: array
          items:
            type: object
            additionalProperties: true
        commentContinuations:
          type: array
          items:
            type: object
            additionalProperties: true
      required:
        - source
        - fetchedAt
        - input
        - post
        - mode
        - comments
    RetryPolicy:
      type: object
      additionalProperties: false
      required:
        - can_retry
        - same_request
      properties:
        can_retry:
          type: boolean
        same_request:
          type: boolean
        retry_after_ms:
          type: integer
          minimum: 0
        idempotency_required:
          type: boolean
      allOf:
        - if:
            required: [can_retry]
            properties:
              can_retry: { const: false }
          then:
            properties:
              same_request: { const: false }
    InvalidField:
      type: object
      additionalProperties: false
      required:
        - path
        - problem
      properties:
        path:
          type: string
          description: Top-level request field path. JSON Pointer (/post) and JSONPath-style ($.post) forms are accepted in
            diagnostics.
        problem:
          type: string
        expected:
          type: string
        received:
          type: string
        suggestion:
          type: string
    RepairPlanStep:
      type: object
      additionalProperties: false
      required:
        - action
        - reason
      properties:
        action:
          type: string
          enum:
            - provide_missing_value
            - replace_invalid_value
            - remove_unknown_field
            - call_prerequisite_operation
            - retry_with_modified_request
            - retry_later
            - do_not_change_request
            - report_diagnostic_id
        path:
          type: string
          description: Top-level request field path. JSON Pointer (/post) and JSONPath-style ($.post) forms are accepted in
            diagnostic repair plans.
        value_hint:
          type: string
        operation_id:
          type: string
        reason:
          type: string
    JsonPatchOperation:
      type: object
      additionalProperties: true
      required:
        - op
        - path
      properties:
        op:
          type: string
          enum:
            - add
            - remove
            - replace
            - move
            - copy
            - test
        path:
          type: string
          description: JSON Patch path. Must use JSON Pointer form such as /post, not JSONPath.
        from:
          type: string
          description: JSON Patch source path for move/copy. Must use JSON Pointer form such as /post, not JSONPath.
        value: {}
    RepairableProblem:
      type: object
      additionalProperties: false
      required:
        - type
        - title
        - status
        - detail
        - instance
        - rec_version
        - operation_id
        - diagnostic_id
        - classification
        - repairable
        - confidence
        - retry_policy
        - caller_instruction
        - safe_debug_summary
        - analysis_mode
      properties:
        type:
          type: string
        title:
          type: string
        status:
          type: integer
          minimum: 400
          maximum: 599
        detail:
          type: string
        instance:
          type: string
          description: Concrete diagnostic occurrence URI in the form urn:diagnostic:<diagnostic_id>.
          example: urn:diagnostic:diag_example_invalid_json
        rec_version:
          type: string
          enum:
            - "1.0"
        operation_id:
          type: string
        diagnostic_id:
          type: string
        trace_id:
          type: string
        classification:
          type: string
          enum:
            - caller_contract_violation
            - semantic_precondition_missing
            - resource_not_found
            - authorization_context_mismatch
            - version_skew
            - dependency_failure
            - capacity_or_timeout
            - service_bug_likely
            - internal_error
            - security_suspicious
            - diagnostic_uncertain
        repairable:
          type: boolean
        confidence:
          type: number
          minimum: 0
          maximum: 1
        retry_policy:
          $ref: "#/components/schemas/RetryPolicy"
        invalid_fields:
          type: array
          items:
            $ref: "#/components/schemas/InvalidField"
        repair_patch:
          type: array
          items:
            $ref: "#/components/schemas/JsonPatchOperation"
        repair_plan:
          type: array
          items:
            $ref: "#/components/schemas/RepairPlanStep"
        correct_request_example: {}
        caller_instruction:
          type: string
        llm_instruction:
          type: string
          description: Optional alias for GPT Actions compatibility.
        safe_debug_summary:
          type: string
        analysis_mode:
          type: string
          enum:
            - deterministic
            - llm_assisted
            - hybrid
            - fallback
    RedditCoverage:
      type: object
      additionalProperties: false
      properties:
        reportedTotal:
          type: integer
        uniqueReturned:
          type: integer
        deleted:
          type: integer
        unavailable:
          type: integer
        knownRemaining:
          type: integer
        cursorsRemaining:
          type: boolean
        continuationsRemaining:
          type: integer
        sortsSampled:
          type: array
          items:
            type: string
        snapshotComplete:
          type: boolean
      required:
        - reportedTotal
        - uniqueReturned
        - deleted
        - unavailable
        - knownRemaining
        - cursorsRemaining
        - continuationsRemaining
        - sortsSampled
        - snapshotComplete
    RedditThreadOverviewRequest:
      type: object
      additionalProperties: false
      properties:
        post:
          type: string
          description: Reddit URL, article ID, t3 fullname, or comment URL.
        url:
          type: string
          description: Alias for post.
        redditUrl:
          type: string
          description: Alias for post.
        reddit_url:
          type: string
          description: Alias for post.
        threadUrl:
          type: string
          description: Alias for post.
        thread_url:
          type: string
          description: Alias for post.
        sort:
          type: string
          enum:
            - confidence
            - top
            - new
            - controversial
            - old
            - qa
          default: confidence
        maxComments:
          type: integer
          minimum: 1
          maximum: 10000
          default: 500
    RedditThreadOverviewResponse:
      type: object
      additionalProperties: true
      properties:
        source:
          type: string
        fetchedAt:
          type: string
          format: date-time
        input:
          type: string
        post:
          type: object
          additionalProperties: true
        stats:
          type: object
          additionalProperties: false
          properties:
            topLevelComments:
              type: integer
            maxDepth:
              type: integer
            deletedCount:
              type: integer
            loadedSnapshotCommentCount:
              type: integer
          required:
            - topLevelComments
            - maxDepth
            - deletedCount
            - loadedSnapshotCommentCount
        availableSorts:
          type: array
          items:
            type: string
        coverage:
          $ref: "#/components/schemas/RedditCoverage"
        redditRateLimit:
          type: object
          additionalProperties: true
      required:
        - source
        - fetchedAt
        - input
        - post
        - stats
        - availableSorts
        - coverage
        - redditRateLimit
    RedditCommentQueryRequest:
      type: object
      additionalProperties: false
      properties:
        post:
          type: string
          description: Reddit URL, article ID, t3 fullname, or comment URL.
        url:
          type: string
          description: Alias for post.
        redditUrl:
          type: string
          description: Alias for post.
        reddit_url:
          type: string
          description: Alias for post.
        threadUrl:
          type: string
          description: Alias for post.
        thread_url:
          type: string
          description: Alias for post.
        sort:
          type: string
          enum:
            - confidence
            - top
            - new
            - controversial
            - old
            - qa
          default: confidence
        limit:
          type: integer
          minimum: 1
          maximum: 500
          default: 200
        cursor:
          type: string
          description: Opaque cursor returned by the previous page.
        includeBody:
          type: boolean
          default: false
        bodyPreviewChars:
          type: integer
          minimum: 0
          maximum: 500
          default: 160
        maxDepth:
          type: integer
          minimum: 0
          maximum: 50
        parentId:
          type: string
          description: Filter to comments whose direct parent is this t1/t3 fullname.
        minScore:
          type: integer
        minBodyLength:
          type: integer
          minimum: 0
        includeDeleted:
          type: boolean
          default: false
        maxBytes:
          type: integer
          minimum: 1000
          maximum: 2000000
          default: 500000
        maxComments:
          type: integer
          minimum: 1
          maximum: 10000
          default: 1000
        maxMoreChildrenRequests:
          type: integer
          minimum: 0
          maximum: 10
          default: 0
    RedditCommentSkeleton:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        fullname:
          type: string
        parentId:
          type: string
        depth:
          type: integer
        score:
          type: integer
        replyCount:
          type:
            - integer
            - "null"
          description: Number of loaded direct replies, or null when the source endpoint does not include reply-tree data (for example comments-batch hydration).
        bodyLength:
          type: integer
        bodyPreview:
          type: string
        createdUtc:
          type: number
        isDeleted:
          type: boolean
        body:
          type: string
      required:
        - id
        - fullname
        - parentId
        - depth
        - score
        - replyCount
        - bodyLength
        - bodyPreview
        - createdUtc
        - isDeleted
    RedditCommentQueryResponse:
      type: object
      additionalProperties: true
      properties:
        source:
          type: string
        fetchedAt:
          type: string
          format: date-time
        input:
          type: string
        post:
          type: object
          additionalProperties: true
        comments:
          type: array
          items:
            $ref: "#/components/schemas/RedditCommentSkeleton"
        page:
          type: object
          additionalProperties: false
          properties:
            nextCursor:
              type:
                - string
                - "null"
            hasMore:
              type: boolean
            returned:
              type: integer
            truncatedBy:
              type:
                - string
                - "null"
              enum:
                - limit
                - maxBytes
                - cursor
                - null
          required:
            - nextCursor
            - hasMore
            - returned
            - truncatedBy
        coverage:
          $ref: "#/components/schemas/RedditCoverage"
        redditRateLimit:
          type: object
          additionalProperties: true
      required:
        - source
        - fetchedAt
        - input
        - post
        - comments
        - page
        - coverage
        - redditRateLimit
    RedditCommentsBatchRequest:
      type: object
      additionalProperties: false
      properties:
        ids:
          oneOf:
            - type: string
            - type: array
              items:
                type: string
          description: Raw Reddit comment IDs or t1 fullnames; maximum 100 unique IDs.
        fields:
          oneOf:
            - type: string
            - type: array
              items:
                type: string
                enum:
                  - id
                  - fullname
                  - parentId
                  - depth
                  - score
                  - createdUtc
                  - body
                  - bodyPreview
                  - bodyLength
                  - replyCount
                  - isDeleted
        maxBytes:
          type: integer
          minimum: 1000
          maximum: 2000000
          default: 500000
      required:
        - ids
    RedditCommentsBatchResponse:
      type: object
      additionalProperties: true
      properties:
        source:
          type: string
        fetchedAt:
          type: string
          format: date-time
        input:
          type: string
        comments:
          type: array
          items:
            type: object
            additionalProperties: true
        found:
          type: array
          items:
            type: string
        missing:
          type: array
          items:
            type: string
        unavailable:
          type: array
          items:
            type: string
        truncatedBy:
          type:
            - string
            - "null"
          enum:
            - maxBytes
            - null
        redditRateLimit:
          type: object
          additionalProperties: true
      required:
        - source
        - fetchedAt
        - comments
        - found
        - missing
        - unavailable
        - truncatedBy
        - redditRateLimit
