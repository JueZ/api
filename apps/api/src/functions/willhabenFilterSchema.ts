import { app, type HttpRequest, type HttpResponseInit, type InvocationContext } from '@azure/functions';
import { authorizeRequest } from '../shared/security/auth.js'; import { WillhabenService,mapWillhabenError } from '../shared/willhaben/service.js';
const service=new WillhabenService(); const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Authorization, Content-Type','Access-Control-Allow-Methods':'GET, OPTIONS'};
export async function willhabenFilterSchemaHandler(request:HttpRequest,context:InvocationContext):Promise<HttpResponseInit>{if(request.method==='OPTIONS')return {status:204,headers:cors}; const auth=await authorizeRequest(request,context); if(!auth.ok) return {...auth.response,headers:{...cors,...auth.response.headers}}; try{return {status:200,headers:cors,jsonBody:service.getFilterSchema(request.query.get('categorySlug')??'')}}catch(e){const m=mapWillhabenError(e); return {status:m.status,headers:{...cors,'Content-Type':'application/problem+json'},jsonBody:{title:m.code,detail:m.message,status:m.status}};}}
app.http('willhabenFilterSchema',{methods:['GET','OPTIONS'],authLevel:'anonymous',route:'api/marketplaces/willhaben/filter-schema',handler:willhabenFilterSchemaHandler});
