import { willhabenConfig } from './config.js';
export function getFilterSchema(categorySlug:string){return {marketplace:'willhaben',categorySlug,filters:{keywords:{type:'array',items:'string',required:true},price:{min:true,max:true,currency:'EUR'},condition:{type:'enum[]',values:['new','like_new','used','broken','exhibition']},fulfillment:{type:'enum[]',values:['pickup','shipping','paylivery']},location:{country:true,city:true,postalCode:true,province:true,radiusKm:true},sort:{values:['relevance','newest','price_asc','price_desc']},pagination:{page:true,limit:true,maxLimit:willhabenConfig.maxResultsLimit}}};}
