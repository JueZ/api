---
name: delivery
description: Complete authorized repository implementation through the project's configured validation and protected delivery path.
---

Inspect the protected base, candidate revision, user changes, requested outcome, and existing delivery authority. Make one coherent change on a separate branch; run proportional local validation and required remote checks for that exact candidate.

For substantial provider, mutation, completeness, or deployment changes, define the observable outcome and use a bounded independent critic to challenge the central assumption. Verify credible counterexamples before delivery; a passing implementation test alone does not establish the requested outcome. Keep straightforward corrections local when independent review adds no useful evidence.

Use the project's configured native merge and delivery mechanism. Unknown commands or permissions are blockers, never successful no-ops. Do not bypass protection or deploy through an improvised path. Respect a local-only handoff request.

Report exact revisions and observed local, merged, deployed, and runtime states. Require the applicable smoke, telemetry, and release-identity evidence before runtime completion. A neutral result needs a trusted classification against the accepted release. For supersession, verify change containment in the current protected revision and follow its generation. Preserve unfinished work and its next action.
