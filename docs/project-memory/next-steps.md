<!-- project-memory-asOf: 2026-08-10 -->
# Next steps

## Delivery efficiency

- For each runtime-neutral merge, verify the associated Main Delivery and exact-main CI metadata directly. Reuse is passing only when the protected validation-profile job succeeds, every full-validation job is explicitly skipped, and `CI complete` succeeds. Do not copy routine successful run narration into memory.
- If reuse fails, inspect only the exact failed verifier/aggregate evidence and use at most two scoped repairs. Never broaden the allowlist or accept an unauthenticated/manual reuse request.

## Programs

- Closed-loop agent learning is complete. The next slice starts only for a genuine post-rollout significant failure, recurrence, or stale-memory contradiction. Keep weekly model-free status/triage and manual dispatch available; do not manufacture incidents or restore model review.
- Quality 10 resumes at the first incomplete unblocked slice in `docs/quality/quality-10-program.md`, currently Phase 1 runtime-environment and local-development-bypass security. Deliver one coherent PR, not the remaining program at once.

## Operational backlog

- Before enabling Bring in test, complete the reviewed private/session migration and verify the dedicated read-only canary identity/list.
- Add a token-safe authenticated MCP smoke only if live MCP provider execution must become a release gate.
- Prove failing/pending aggregate and missing deterministic-governance merge denial with genuine bounded canaries.
- Reduce the Angular initial bundle before growth makes the current warning material.
- Leave obsolete zero-job Actions records documented unless GitHub provides a safe deletion mechanism.
- Real historical agent-task execution remains optional, explicitly authorized, isolated, sanitized, model/credential bounded, and never required CI.
