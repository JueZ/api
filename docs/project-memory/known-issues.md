<!-- project-memory-asOf: 2026-08-10 -->
# Known issues and unresolved risks

## Learning and historical-evaluation evidence gaps

- No significant `codex-repair` issue has occurred after the Phase 3 rollout timestamp. Live first-candidate creation and recurrence linking await a genuine event; deterministic coverage exists and no historical flood was created.
- No real or paid agent-task evaluation is authorized or evidenced. Historical/current-context pass rates remain unavailable. Never reuse a production/provider credential or make paid evaluation branch-required.
- Open learning issues #359 and #374 retain durable follow-up work for historical delivery-lineage and identical-compare recurrence. They do not reopen the accepted five-phase program.

## GitHub platform remnants

- Actions runs `30663819848` and `30693764586` are obsolete zero-job records that GitHub would not cancel or delete. They cannot execute and should remain documented unless GitHub later exposes a safe deletion path.

## Bring rollout boundary

- Test Bring remains disabled. Remaining private/session data needs reviewed inventory, backup, copy, digest comparison, access verification, and rollback before enablement.
- The GET-only canary requires a dedicated `bring.read` identity and verified target list. Never add a mutation canary. Test intentionally reads the configured account when enabled.

## Runtime/product limitations

- Angular production output remains above the configured initial-bundle warning threshold; this is a non-fatal optimization item.
- Reddit may block some short share URLs and very large threads remain bounded/partial by design. The per-worker concurrency gate is not distributed; add shared quota/asynchronous export only if telemetry proves current limits insufficient.
- A token-safe authenticated MCP provider smoke is not a formal deployment gate. Existing authenticated REST smoke and deterministic MCP authorization coverage remain active.
- Live canaries should still prove an intentionally failing/pending check and missing high-risk deterministic governance cannot merge. Existing protected logic and regression tests fail closed, but the remaining live observations must not weaken checks or manufacture success.
