# Documentation scope instructions

- Distinguish local implementation, merged code, deployed infrastructure, and verified runtime state.
- Never claim planned/local settings are live without GitHub/Azure/runtime evidence.
- Record durable architecture/security/deployment decisions in project memory without secrets.
- Keep generated operation documentation synchronized with the canonical registry.
- Use `docs/cost/` for new paid or always-on services and `docs/adr/` for durable tradeoffs.
