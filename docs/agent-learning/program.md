# Closed-loop agent-learning program

As of 2026-08-09, this document is the authoritative, resumable ledger for the JueZ/api agent-learning program.

## Objective

Build a production-grade closed loop that evaluates broad repository tasks and agent behavior, converts significant failures into structured learning candidates, turns approved candidates into versioned verifiable artifacts, evaluates historical tasks end to end, measures the effect of repository instructions and skills, and reports memory freshness without weakening any existing security, CI, delivery, authentication, authorization, provenance, smoke, telemetry, audit, idempotency, or production control.

Accepted phase statuses are `not_started`, `in_progress`, `accepted`, `blocked`, and `superseded`.

## Evidence rules

- Local checks, exact-head PR checks, merge, deployment, smoke, telemetry, release-ledger validation, runtime truth, and live repository configuration are separate evidence classes.
- A merged PR alone is not runtime evidence and is not proof that a live GitHub or Azure setting changed.
- Every accepted reference must identify the exact 40-character commit and the relevant PR, workflow run, artifact, or live read-back.
- Unavailable evidence is `blocked` or pending; it is never counted as passing.
- Evidence stored under `docs/agent-learning/evidence/` must be public-safe and exclude tokens, headers, credentials, connection strings, SAS URLs, provider data, and unrelated settings.

## Program status

| Phase | Scope                                              | Status        | PR and exact commit references                                                                                                                                                 | Accepted evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                | Remaining risk                                                                                                                                                                                                                                 |
| ----- | -------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1     | Stable branch-protection aggregation               | `accepted`    | PR #345 head `06a68d42e205ec38ec8c0fb906e59dd9fc8ce415`, merged as `da8459aec5756f684b27d692dd838b0135c7fe9f`; canary PR #346 head `f777ac527ca73e86886b2579aca5ce93ed0ed836`. | All four aggregates passed on the exact implementation head under the old 23-context protection; main delivery, test and production runtime truth passed; the live protection read-back contains exactly four strict aggregate contexts; PR #346 proved an internal failure blocks `CI complete`, merge, and paid review. Public-safe evidence: `docs/agent-learning/evidence/branch-protection-aggregation.json`.                                               | Future workflow changes must keep aggregate `needs` coverage explicit and update its deterministic tests. The controller must continue evaluating the complete latest check/status rollup, not only the four protected contexts.               |
| 2     | Versioned learning artifacts and closed-loop skill | `accepted`    | PR #349 head `7188188cc0b3fd1a58a5ee14ae5335158294135c`, merged as `9310c94f97541e57f83b186af2cacf989d6f5330`.                                                                 | The exact head passed all four protected aggregates; trusted scorers verified all three historical broken/fixed Git objects and live PR provenance; main CI and staged delivery passed; an authenticated protected-CI verifier binds exact checks, workflow runs, release ledgers, distinct GitHub test/production deployments, and live `/health` identity to the merge. Public-safe evidence: `docs/agent-learning/evidence/phase-2-versioned-artifacts.json`. | Historical proof validates repository invariants without executing old code. General detached-worktree agent execution, context-variant performance measurement, and automatic failure-to-candidate conversion remain owned by Phases 3 and 4. |
| 3     | Automatic failure-to-learning conversion           | `not_started` | None.                                                                                                                                                                          | None.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Significant failures do not yet create deduplicated learning candidates automatically.                                                                                                                                                         |
| 4     | General historical agent-task evaluation harness   | `not_started` | None.                                                                                                                                                                          | None.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | No detached-worktree adapter/scorer harness or context-variant comparison yet.                                                                                                                                                                 |
| 5     | Memory freshness and day-to-day reporting          | `not_started` | None.                                                                                                                                                                          | None.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | No deterministic freshness report or scheduled closed-loop status summary yet.                                                                                                                                                                 |

## Phase 1 accepted evidence

Phase 1 was accepted after all of the following were verified independently:

1. PR #345's exact head passed `CI complete`, `Policy complete`, `CodeQL complete`, and `Autonomous review complete` under the pre-existing 23-context protection and old main controller.
2. Main delivery `31276530181`, exact-main CI `31276543255`, Deploy Test `31276618522`, and Promote Production `31276791354` succeeded for exact merge `da8459aec5756f684b27d692dd838b0135c7fe9f`.
3. Fresh authenticated runtime-truth checks combined live `/health` with exact release-ledger artifacts and verified the exact merge in test and production; public smoke, authenticated smoke, telemetry correlation, and ledger validation passed in both environments.
4. Only the live required-status-check endpoint was changed. Read-back found strict protection with exactly `CI complete`, `Policy complete`, `CodeQL complete`, and `Autonomous review complete`; pull-request enforcement, admin enforcement, linear history, conversation resolution, force-push denial, and deletion denial were preserved.
5. Canary PR #346 deterministically failed `lint`, which failed `CI complete`; GitHub remained blocked, the free-gate wait stopped autonomous review before its paid step, the merge job was skipped, the PR was closed unmerged, and its remote branch was deleted.
6. Public-safe before/after, delivery, runtime, and canary evidence is archived at `docs/agent-learning/evidence/branch-protection-aggregation.json`.

## Phase 2 accepted evidence

Phase 2 was accepted after all of the following were verified independently:

1. PR #349 exact head `7188188cc0b3fd1a58a5ee14ae5335158294135c` passed `CI complete` in run `31279530179`, `Policy complete` in run `31279530178`, `CodeQL complete` in run `31279530171`, and `Autonomous review complete` in run `31279529529`, then merged through protected delivery as `9310c94f97541e57f83b186af2cacf989d6f5330`.
2. Fixed-path CI validation accepted the strict artifact schema, reproducible checked-in index, deterministic malformed/duplicate/traversal/stale/waiver/verified regression tests, skill validation, and trusted historical proof. The trusted scorers inspected exact Git objects and commit ancestry without executing historical code, while authenticated GitHub validation bound PRs #321, #324, and #344 to their exact base and merge SHAs.
3. Three historical learning artifacts are `verified`, zero are waived, and their repository-contained prevention artifacts exist. Review failure candidate `runtime-evidence-environment-binding` is `implemented` with a deterministic regression and remains linked to issue #351 until fixed-commit proof is accepted. `eval:agents` remains compatible; Phase 4 task-evaluation aliases fail closed rather than reporting adapter absence as a pass.
4. Main Delivery `31279667347`, exact-main CI `31279682790`, Deploy Test `31279752243`, and Promote Production `31279913572` succeeded for the exact merge. Both environments passed public smoke, authenticated smoke, telemetry correlation, and release-ledger publication.
5. Fresh runtime-truth checks at `2026-08-08T21:47:09.509Z` for test and `2026-08-08T21:47:22.736Z` for production combined live `/health` with the exact workflow and ledger identity and verified `9310c94f97541e57f83b186af2cacf989d6f5330` without failures or blockers.
6. The Phase 2 evidence transition is itself fail closed: when the evidence record changes, the existing fixed CI entry point requires an authenticated GitHub token, binds each aggregate check to the exact PR head and workflow run, binds delivery to immutable workflow paths and the merge SHA, authenticates the exact successful deployment job and ledger-publication steps, downloads the recorded artifact ID directly, and verifies the received bytes against GitHub's authenticated SHA-256 digest before parsing the canonical ledger. That digest-bound ledger environment/origin is the run-bound attestation and the only source for the live `/health` request. GitHub exposes no immutable deployment-to-job field, so status `log_url` is only corroboration; authenticated deployment/status records cross-check the expected environment and origin. Test and production attested origins must differ, and live health must match the environment, run, and merge SHA. The `pull_request_target` REST source head/branch is checked separately from authenticated PR base/head and review-check identity. Editable JSON fields alone are not proof.
7. No paid general agent-task evaluation ran and no historical learning-issue flood was created. Public-safe implementation, repair, delivery, runtime, and validation evidence is archived at `docs/agent-learning/evidence/phase-2-versioned-artifacts.json`.

## Next exact slice

Implement Phase 3 as one high-risk PR by extending the existing repair-triage architecture with deterministic learning-disposition, fingerprint, stable-marker, deduplication, recurrence, candidate-body, and explicit-exception validation. Add the rollout timestamp, exact-range dry-run backfill, idempotent least-privilege write mode, labels, linkage/closure rules, regression tests, trusted workflow hashes, docs, and project-memory updates. Do not begin the detached-worktree task harness or any paid general evaluation in this slice.
